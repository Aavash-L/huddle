# Hoist next to root node_modules so Vercel's framework detection
# can find it when building from the monorepo root (no Root Directory override).
public-hoist-pattern[]=next
public-hoist-pattern[]=react
public-hoist-pattern[]=react-dom
